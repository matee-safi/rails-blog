<div class="user no-hover-effect rounded row align-items-center m-5 mb-1 p-2">
  <%= image_tag(@user.photo, alt: "user image", class: "col-3 user-images") %>
  <div class="justify-content-start mb-5 col-6">
    <h1><%= @user.name %></h1>
  </div>
  <h6 class="align-items-end col-3">Number of posts: <%= @user.posts_counter %></h6>
</div>
<div class="posts p-5">
    <% @user.posts.each do |post| %>
    <%= link_to user_post_path(@user.id, post.id) do %>
        <div class="shadow-white rounded border p-3 my-4 post">
            <h4 class="mb-4"><%= post.title %></h4>
            <p><%= post.text %></p>
            <div class="d-flex w-100 justify-content-end">
                <h6>Comments: <%= post.comments_counter %>, Likes: <%= post.likes_counter %></h6>
            </div>
            <% if post.comments.length > 0 %>
                <div class="comments border-top pt-3 mt-3">
                    <% post.recent_comments.each do |comment| %>
                        <p><%= comment.author.name %>: <%= comment.text %></p>
                    <% end %>
                </div>
            <% end %>
        </div>
    <% end %>
    <% end %>
</div>
